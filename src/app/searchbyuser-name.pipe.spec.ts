import { SearchbyuserNamePipe } from './searchbyuser-name.pipe';

describe('SearchbyuserNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchbyuserNamePipe();
    expect(pipe).toBeTruthy();
  });
});
